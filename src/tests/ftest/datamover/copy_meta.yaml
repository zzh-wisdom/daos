hosts:
    test_servers:
        - server-A
    test_clients:
        - server-B
timeout: 180
server_config:
    name: daos_server
pool:
    mode: 146
    name: daos_server
    scm_size: 1G
    control_method: dmg
container:
    type: POSIX
    control_method: daos
dcp:
    client_processes:
        np: 1
dfuse:
    mount_dir: "/tmp/daos_dfuse"
